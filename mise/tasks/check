#!/usr/bin/env bash
#MISE description="Run every local/CI check and package every extension"
#MISE depends=["lint", "typecheck", "test", "pack"]

set -euo pipefail
echo "All checks passed; installable extensions are in dist/."
